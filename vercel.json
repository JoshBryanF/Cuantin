{
  "framework": "laravel",
  "functions": {
    "api/index.php": {
      "memory": 3008,
      "maxDuration": 30
    }
  },
  "env": {
    "APP_ENV": "production",
    "APP_DEBUG": "false"
  },

  "buildCommand": "composer install && npm run build && php artisan migrate --force"
  
}